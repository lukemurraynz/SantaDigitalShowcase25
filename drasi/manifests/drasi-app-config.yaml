apiVersion: v1
kind: ConfigMap
metadata:
  name: drasi-app-config
  namespace: drasi-system
  labels:
    app: drasi-controller
data:
  API_BASE_URL: "REPLACE_WITH_API_URL"
  COSMOS_DATABASE: "elves_demo"
  EVENTHUB_FQDN: "drasicrhsith-jhsd1-eh.servicebus.windows.net"
  # These values are replaced by apply-drasi-resources.ps1 during deployment.
  # API_BASE_URL from Container App FQDN, EVENTHUB_FQDN from EventHub namespace.
