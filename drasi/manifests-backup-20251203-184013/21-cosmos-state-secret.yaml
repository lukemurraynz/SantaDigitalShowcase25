apiVersion: v1
kind: Secret
metadata:
  name: cosmos-statestore
  namespace: drasi-system
type: Opaque
stringData:
  # Placeholder endpoint - replaced during deployment by apply-drasi-resources.ps1.
  # Managed Identity authentication is used; no key needed.
  endpoint: https://REPLACE_COSMOS_ACCOUNT.documents.azure.com:443/
